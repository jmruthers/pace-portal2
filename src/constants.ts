/** pace-portal application name for RBAC and shell (PR01). */
export const APP_NAME = 'pace';

/** localStorage key for delegated proxy target member (PR02). */
export const PROXY_TARGET_MEMBER_STORAGE_KEY = 'pace-portal:proxyTargetMemberId';
