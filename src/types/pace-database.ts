/**
 * Supabase schema types from pace-core (generated).
 * Uses a workspace-relative path because the package does not export `Database` on the public `types` entry.
 */
export type { Database } from '../../node_modules/@solvera/pace-core/dist/types/database.generated.js';
