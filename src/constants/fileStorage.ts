/** Supabase bucket for profile photos and medical action plans (PR03, PR11). */
export const FILE_STORAGE_BUCKET = 'files';

/** Canonical storage path segment when an upload is not scoped to an event. */
export const FILE_UPLOAD_NO_EVENT_ID = 'no-event';

/** Canonical storage path segment when organisation context is not yet available. */
export const FILE_UPLOAD_NO_ORG_ID = 'no-org';
